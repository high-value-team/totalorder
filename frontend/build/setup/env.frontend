# These placeholder variables will be used when creating a docker image and will later be replaced when the container is started.
REACT_APP_BACKEND_URL = FRONTEND_ENVIRONMENT_VARIABLE_PLACEHOLDER_REACT_APP_BACKEND_URL